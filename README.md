# Twitter Stream Raspberry

To run application:

- Install NodeJS
- Rename config.js.template to config.js and populate your Twitter API keys
- Run `npm install -g nodemon`
- Run `npm install`
- Run `nodemon index.js`
- Go to `http://localhost:3000`
